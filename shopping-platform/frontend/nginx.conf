server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # RAG Agent: /api/cart, /api/orders, /api/auth
  location ~ ^/api/(cart|orders|auth) {
    proxy_pass http://rag-agent:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    proxy_cache off;
  }

  # RAG Agent: /agent -> /api (SSE streaming)
  location /agent/ {
    rewrite ^/agent/(.*)$ /api/$1 break;
    proxy_pass http://rag-agent:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 300s;
  }

  # RAG Agent: /agent/static -> /static
  location /agent/static/ {
    rewrite ^/agent/(.*)$ /$1 break;
    proxy_pass http://rag-agent:8000;
    proxy_set_header Host $host;
  }

  # Java Platform: /api/* -> http://java-backend:8080/*
  location /api/ {
    rewrite ^/api/(.*)$ /$1 break;
    proxy_pass http://java-backend:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
